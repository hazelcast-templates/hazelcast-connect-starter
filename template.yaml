defaults:
  project_group: "com.example"
  project_name: "sampleapp"
  project_version: "0.1.0"
  flow_version: "5.5.0"
  mc_version: "5.6.0"
  postgres_password: "43882f197-a23957b"

note: |
  Hazelcast Flow workspace was created successfully.
  
  You can start Hazelcast Flow using the following steps:
  - cd into the project directory.
  - Set up the Hazelcast Flow license in .env
  - Run: docker compose up
  Please check out the README.md for more details!
 
  Hazelcast Flow is available at http://localhost:9021
